// בדיקת בעיות פיסוק וצמידות גרשיים - גרסה סופית
function testPunctuationFinal() {
  console.log('🧪 Testing punctuation issues with correct logic...');

  const testCases = [
    'הוא אמר"אני הולך הביתה".',
    'הספר"תורה" נמצא על השולחן.',
    '"מה אתה אומר"שאל הרב.',
    'אמר"שלום."והלך.',
    'הם קראו"שמע ישראל".היום.',
    '"ברוך השם"אמר הילד.',
  ];

  testCases.forEach((testCase, index) => {
    console.log(`\\n--- Test ${index + 1} ---`);
    console.log('לפני:', testCase);

    let text = testCase;

    // שלב 1: תיקון גרשיים פותחים צמודים למילה עברית (מילה"טקסט -> מילה "טקסט)
    // אבל רק אם זה לא תחילת שורה או אחרי רווח
    text = text.replace(/([א-ת])"([א-ת][^"]*)/g, '$1 "$2');

    // שלב 2: תיקון גרשיים סוגרים צמודים למילה הבאה
    // זיהוי גרשיים סוגרים: יש תוכן לפני הגרשיים ואין עוד גרשיים אחריהם מיד
    text = text.replace(/([א-ת][^"]*[א-ת])"([א-ת])/g, '$1" $2');

    // שלב 3: תיקון מקרים של נקודה אחרי גרשיים (צריכה להיות לפני)
    text = text.replace(/([א-ת])\\."/g, '$1".');

    // שלב 4: הוספת רווח אחרי נקודה+גרשיים כשהם צמודים למילה
    text = text.replace(/"\\.([א-ת])/g, '". $1');

    // שלב 5: תיקון מקרים נוספים של גרשיים סוגרים
    // אם יש נקודה או פסיק לפני הגרשיים, זה כנראה גרשיים סוगרים
    text = text.replace(/([.,!?])"([א-ת])/g, '$1" $2');

    // נקה רווחים מיותרים
    text = text.replace(/\\s{2,}/g, ' ');

    console.log('אחרי:', text);
  });
}

testPunctuationFinal();